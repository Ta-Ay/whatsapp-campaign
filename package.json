{
  "name": "whatsapp-campaign-server",
  "version": "1.0.0",
  "description": "Proxy + Webhook server for WhatsApp 360dialog campaigns",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
